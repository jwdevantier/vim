Be sure to get Neovim 0.5+

On ubuntu:


sudo snap install nvim --edge --classic

If already installed, replace 'install' with 'refresh'


Read the header of each config/ file, e.g. coc.vim requires some setup work
before working.

Use ":checkhealth" to check if anything obvious is wrong.

